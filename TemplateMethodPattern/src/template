模板方法模式介绍：
	1）模板方法模式又叫模板模式，在实现一个抽象类公开定义了执行它的方法的模板。它的子类可以按需要重写方法实现，但调用将以抽象方式进行
	2）模板方法模式定义了一个操作中算法的骨架，而将一些步骤延迟到子类中，使得子类可以不改变一个算法的结构就可以重新定义算法的某些特定步骤
	3）这种模式属于行为型模式
	
	模板方法模式中的钩子方法：
		在模板方法模式的父类中，我们可以定义一个方法，它默认不做任何事，子类可以视情况要不要覆盖它，该方法成为"钩子"
	
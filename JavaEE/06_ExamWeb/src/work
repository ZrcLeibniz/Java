任务：在线考试管理系统----用户信息管理模块

子任务：
    用户信注册
    用户信息查询
    用户信息删除
    用户信息更新

准备工作：
    1.创建用户信息表 Users.frm
    CREATE TABLE Users(
        userID int primary key auto_increment,
        userName varchar(50),
        password varchar(50),
        sex char(1),
        email varchar(50)
    )

    2.在src下，建立与数据库表对应的实体类

Http状态码：
    1.介绍：
        1）由三位数字组成的一个符号
        2）Http服务器在推送响应包之前，根据本次请求处理情况将Http状态码写入响应包的【状态行】上
        3）如果Http服务器针对本次请求返回，返回了对应的资源文件。通过Http状态码通知浏览器应该如何处理这个结果
           如果Http服务器针对本次请求，无法返回对应的资源文件，通过Http状态码向浏览器解释不能提供服务的原因
    2.分类
        1）组成：100--->599之间的数字组成；分为五个大类
        2）1XX：
            最有特征 100：通知浏览器本次返回的资源文件并不是一个独立的资源文件，需要浏览器在接受响应包之后，继续向Http服务器索要
            依赖的其他资源文件
        3）2XX
            最有特征 200：通知浏览器本次返回的资源文件是一个完整独立资源文件，浏览器在接受到之后不需要索要其他资源文件
        4）3XX
            最优特征 302：通知浏览器本次返回的不是一个资源文件的内容，而是一个资源文件的地址，需要浏览器根据这个地址自动发起请求
            来索要这个资源文件
        5）4XX
            最有特征 404：通知浏览器由于在服务端没有定位到被访问的资源文件，因此无法提供帮助
                    405：通知服务器，在服务端已经定位到被访问的资源文件（Servlet），但是这个Servlet对于浏览器采用的请求方式不能
                    处理
        6）5XX
            最有特征 500：在服务端已经定位到被访问的资源文件（Servlet），这个Servlet可以接受浏览器采用的请求方式，但是Servlet在
            处理请求期间，由于Java异常导致处理失败

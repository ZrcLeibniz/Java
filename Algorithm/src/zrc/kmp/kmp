字符串匹配
	暴力匹配法：
		1）如果当前字符匹配成功（即str1[i]=str2[j]），则i++，j++，继续匹配下一个字符
		2）如果失配（即str1[i]！=str2[j]），令i=i-（j-1），j=0。相当于每次匹配失败时，i回溯，j被置为0
	KMP：
		1）先得到字串的部分匹配表
		2）使用部分匹配表完成KMP的匹配：移动位数 = 已匹配的字符数 - 对应的部分匹配值
		